# app

このディレクトリは Vite で構成されたアプリ本体（フロントエンド）のソースです。

## 開発/ビルド

`app/` ディレクトリ内で実行します。

```bash
npm install
npm run dev
npm run build
npm run preview
```

詳細な説明や運用（GitHub Pages へのデプロイ、データ形式など）は `../README.md` を参照してください。
